// src/i18n-config.ts
// This file is now only a fallback for server-side route matching.
// The primary source of truth for languages is the database.
// The UI will dynamically load available languages and will not be limited by this config.

export const locales = ["en", "es"];
export const defaultLocale = 'es';
