// This is the root layout that applies to all locales.
// It should only contain the <html> and <body> tags.

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
